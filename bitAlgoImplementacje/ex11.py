# Zaproponuj algorytm, który w czasie O(nlog(n)) posortuje stos o rozmiarze n. 
# Dozwolone jest tylko wykorzystywanie operacji udostępnionych przez interfejs stosu:
# push(), pop(), top(), isEmpty(), oraz dodatkowych stosów.

#taka linked lista fajen jest